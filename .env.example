# Puerto del servidor
PORT=3000

# Base de datos PostgreSQL (Render External Database URL)
DATABASE_URL=postgresql://usuario:password@localhost:5432/eventspace_db

# JWT - clave secreta para firmar tokens (mínimo 32 caracteres)
JWT_SECRET=mibombo_universidad_render_free

# Stripe - modo test (claves que empiezan por pk_test_ y sk_test_)
STRIPE_PUBLISHABLE_KEY=pk_test_tu_clave_publicable
STRIPE_SECRET_KEY=sk_test_tu_clave_secreta

# Origen permitido para CORS (mismo origen si frontend está en Express)
FRONTEND_URL=http://localhost:3000

# Variables adicionales del proyecto (configurar en Render)
EMAIL_PASSWORD=tu_contraseña_de_email
EMAIL_USER=tu_email@gmail.com
GOOGLE_CLIENT_ID=tu_google_client_id
GOOGLE_CLIENT_SECRET=tu_google_client_secret
KEYAPIFAKE=tu_key_api_fake
KEYIPAPI=tu_key_ip_api
NODE_ENV=production
SECRET_KEYY=tu_secret_key_reCaptcha
SESSION_SECRET=una_clave_segura_para_sesiones
SITIO_KEY=tu_sitio_key_reCaptcha
